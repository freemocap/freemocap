# Archive — 2026-08-24 skellyforge rebuild

Dead-generation records moved aside when the plan folder was reconciled to the second-generation core
(VRM-aligned 61-segment model, closed-form hydration + parallel-transport roll, derived biomechanics
layer). Everything in here describes deleted APIs — `HumanSkeleton`, `StandardHumanTPose`,
`build_standard_human_tpose`, `solve_frame_orientations`, `rigidify_landmarks`, the damped/swing-twist
tiers, 95-segment counts, Y-mirroring. History only — never guidance. Live scope:
[../../IMPLEMENTATION_PLAN.md](../../IMPLEMENTATION_PLAN.md).
